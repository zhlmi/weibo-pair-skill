# 微博配对 · 用户指南

> 你的微博暴露了怎样的你？你和 TA 在数据里合拍吗？

**微博配对**是一个基于微博公开数据的关系分析工具。两个入口：

- **自画像**（免费）：看自己——七维面相 + 荒诞判词
- **双人配对**（需 CLI 套餐）：看两个人——八维匹配 + 相处建议 + 缘分指数

---

## 目录

1. [前置条件](#前置条件)
2. [自画像](#自画像)
3. [双人配对](#双人配对)
4. [输出模板](#输出模板)
5. [数据量与限流](#数据量与限流)
6. [结果解读](#结果解读)
7. [常见问题](#常见问题)
8. [免责声明](#免责声明)

---

## 前置条件

### 依赖

| 工具 | 作用 | 安装 |
|------|------|------|
| `weibo-skill` | 自画像 + 配对我方 ≤100 条数据（免费） | 向 @微博龙虾助手 私信发送"连接龙虾"获取 App ID 和 Secret，然后运行 `node weibo-skill.js login --app-id=<ID> --app-secret=<SECRET>` |
| `weibo-cli` | 配对对方数据（付费） | `npm i -g @weibo-ai/weibo-cli`（需 Node.js） |

**iSH 环境特别注意**：weibo-cli 默认自带的 undici 版本与 iSH 网络栈不兼容，需替换为 undici 8.7.0。详见 [references/data-sources.md](references/data-sources.md) 的"数据采集规范"章节。

### Token

仅需一个环境变量：

```bash
export WEIBO_CLI_TOKEN="wb_xxxxxxxxxxxxxxxx"   # weibo-cli 的长期 Token，必填
```

Token 在微博开放平台控制台获取。

### 自检

首次使用前请确认：

```bash
weibo-cli doctor          # 应显示 登录账号 / 开发者认证 / 套餐 全部 ✓
echo $WEIBO_CLI_TOKEN     # 应输出 Token
node weibo-skill.js status # 应返回自己的微博列表
```

---

## 自画像

> 你的微博就是你的面相。

### 用法

```bash
python3 scripts/self-portrait.py [选项]
```

| 参数 | 说明 | 默认 |
|------|------|------|
| `--count 100` | 采样条数（≤100 免费 weibo-skill；>100 走 CLI） | 100 |
| `--output 面相.html` | 输出路径 | `<缓存目录>/self-portrait.html` |
| `--template portrait` | 模板：`portrait`=面相诊断书 / `mingpan`=星空罗盘动效版 | portrait |
| `--on-short ask` | 数据不足策略：`ask`=询问 / `continue`=继续 / `abort`=中止 | ask |
| `--cache-dir 目录` | 缓存目录 | `$WEIBO_PAIR_CACHE` 环境变量 或 `/var/minis/workspace`（Minis 默认） |

### 结果

一份**数字面相诊断书**或**星空罗盘**，包含：

- **七维面相**：额相（思维）、眼相（焦点）、气色（情绪）、口相（表达）、骨相（存在）、手相（创作）、眉相（提问），每个维度一个面相术语 + 荒诞判词
- **综合判语**：七相凝练的一段话，描述你的"微博人格"
- **运势建议**：荒诞但有洞察的医嘱

每次运行判词措辞随机变化，不会两次一模一样。

---

## 双人配对

> 你们的微博数据告诉宇宙，你们配不配。

### 用法

```bash
python3 scripts/pair.py --other-uid <对方UID> --my-uid <我方UID> [选项]
```

**必选参数**：

| 参数 | 说明 |
|------|------|
| `--other-uid <UID>` | 对方微博 UID（数字） |
| `--my-uid <UID>` | 我方微博 UID（数字） |

**可选参数**：

| 参数 | 说明 | 默认 |
|------|------|------|
| `--count 100` | 对方采样条数 | 100 |
| `--my-count 100` | 我方采样条数（≤100 free，>100 CLI） | 100 |
| `--mode friend` | 分析方向：`couple`=情侣 / `friend`=朋友 / `business`=商务 | friend |
| `--output 报告.html` | 输出路径 | `<缓存目录>/weibo-pair-match.html` |
| `--my-nick <昵称>` | 我方昵称（缺省自动获取） | - |
| `--other-nick <昵称>` | 对方昵称 | - |
| `--my-role <身份>` | 我方身份描述（缺省按话题推断） | - |
| `--other-role <身份>` | 对方身份描述 | - |
| `--mutual-follow` | 确认双方互关（自动检测受 API 限制，用户确认兜底） | - |
| `--polarity-note <文案>` | 关系事实（如"线下老朋友"），影响判词 | - |
| `--on-short ask` | 数据不足策略：`ask`=询问 / `continue`=继续 / `abort`=中止 | ask |
| `--cache-dir 目录` | 缓存目录 | 同上 |

### A×B 配对（帮任意两人配对）

如果你要配对**两个都不是你本人**的账号（如"小张×小李合不合"）：

```bash
python3 scripts/pair.py --other-uid <B方UID> --my-uid <A方UID> --my-count 50 --count 50
```

A×B 时双方数据均走 CLI（都计费），互关自动检测标"API 受限"（可用 `--mutual-follow` 人工确认），圈子兼容降级为内容圈层近似。详见 [references/matchmaking.md](references/matchmaking.md)。

### 结果

一份包含以下内容的 HTML 报告：

- **两人画像对比**：活跃时段、话题偏好、情绪基调、创作频次
- **八维匹配度**：作息匹配、兴趣契合、性格互补、沟通风格、创作互补、笑点匹配、圈子兼容、互动深度（缘分指数）
- **相处建议**：三条针对性的点评（按模式：情侣暧昧 / 朋友轻松 / 商务克制）

---

## 输出模板

> 所有 HTML 固化模板的设计方向，来自 [花叔设计 Skill（huashu-design）](https://github.com/alchaincyf/huashu-design)——以 HTML 原生方法论做高保真视觉输出的设计工具体系。自画像诊断书、星空罗盘即从花叔三方向初稿中选定并迭代打磨而来；双人配对三模板的反 AI slop 原则（禁紫渐变/emoji 图标/圆角卡片等）亦参照花叔规范。

### 自画像

| 模板 | 视觉风格 | 参数 |
|------|---------|------|
| **面相诊断书** | 冷灰病历 + 红色「面相科」章 + 化验单表格 | `--template portrait`（默认） |
| **星空罗盘** | 深蓝夜空 + 七相环绕 + 环转/星闪动效 + 桌面鼠标视差 | `--template mingpan` |

### 双人配对

| 方向 | 视觉风格 | 参数 |
|------|---------|------|
| **情侣** | 相亲角报纸：宣纸米白 + 朱砂红印章 + 竖排题字 + 媒婆点评 | `--mode couple` |
| **朋友**（默认） | 电影片尾：暖黑底 + 琥珀监视器光 + 卡司表 + 电平表 + 场记点评 | `--mode friend` |
| **商务** | 数据杂志：Swiss 克制排印 + 发丝网格 + 巨型数字 + 互动深度特写 | `--mode business` |

所有模板均支持手机响应式（@media 760px / 400px 断点）。

---

## 数据量与限流

CLI 读取接口限制 **60 次/小时**，**整点重置**（如 18:00、19:00 整点清零）。

| 条数 | CLI 调用次数 | 适合套餐 | 单窗口可跑？ |
|------|-------------|---------|-------------|
| 100 条 | 0（weibo-skill 免费） | FREE+ | ✅ |
| 200 条 | ~10 次 | BASIC 以上 | ✅ |
| 500 条 | ~30 次 | PLUS 以上 | ✅ |
| 800+ 条 | 40+ 次 | ULTRA | ⚠️ 需跨整点两段跑 |

脚本内置**断点续传**：中途限流/失败自动落盘，重跑从缓存末尾续传，不重复烧调用。数据不足目标时会询问你：继续 / 等整点重跑。

### 缓存

所有已采集数据自动缓存（按 UID 隔离，不同用户/场次互不覆盖）。缓存目录通过 `--cache-dir` 或 `$WEIBO_PAIR_CACHE` 环境变量指定。

缓存文件：

- `pair_other_posts_<UID>.json` — 对方微博
- `pair_my_posts_<UID>.json` — 我方微博
- `pair_my_following_<登录UID>.json` — 关注列表（约 200 人）
- `pair_active_users_<UID>.json` — 活跃关注样本
- `pair_login_uid.json` — 登录用户 UID

---

## 结果解读

### 缘分指数（双人配对）

缘分基于**真实互动信号**扫描：

| 互动类型 | 权重 | 说明 |
|---------|------|------|
| 直接转发对方原创 | 0.45/次 | 我/对方直接转发对方的原创微博 |
| 回复对方 | 0.35/次 | 直接回复对方的微博 |
| @提及对方 | 0.20/次 | 独立 @（非转发链格式） |
| 转发链共现（技术内容） | 0.15/条 | 同一条传播链里出现对方，且内容含技术关键词 |
| 转发链共现（通用） | 0.10/条 | 同一条传播链里出现对方 |
| 互关 | 0.10 | 确认双方互相关注 |

**密度归一**（v1.7.0）：缘分 = 每百条加权互动次数（而非绝对次数），不同样本量的场次之间可比。高频博主的海量日常发博不会稀释互动信号。

**互动极性**：扫描负面词数量——如果负面词指向对方内容（如对线/开骂），缘分打折；吐槽第三方（工作/项目/甲方）不算摩擦。用户可用 `--polarity-note` 确认关系事实，跳过自动极性判定。

### 面相判词（自画像）

七维面相术语全部有多个文案候选（每术语 3 条变体），总判语有 3 套句式骨架，医嘱有 81 种组合。每次运行随机选取，保证不同用户、不同时间跑出来的文案都不一样。

---

## 常见问题

**Q: 配对结果 0 分 / 缘分全是 0？**
A: 检查对方 UID 是否正确、账号是否公开。若对方微博量极少（<20 条），报告会标注"数据不足"。

**Q: 数据拉到一半报错了？**
A: 脚本自动落盘已抓数据，重跑会自动续传。若反复限流，等整点（:00）后重试。数据不足时会询问你："继续用现有数据"或"等整点重跑"。

**Q: A×B 配对（两人都不是自己）能跑吗？**
A: 支持。`--my-uid` 传 A 方 UID、`--other-uid` 传 B 方 UID。双方均走 CLI（都计费），互关自动检测标"API 受限"（可用 `--mutual-follow` 人工确认）。

**Q: 两个人用了同样的参数，跑出来得分不一样？**
A: 文案有随机变体（判词/点评措辞），但数据判定稳定。得分只会因采样量不同而变化（更全的样本 → 更准的分数）。

**Q: 我能用自己的身份描述覆盖自动推断吗？**
A: 可以，`--my-role "摄影师" --other-role "编剧"` 覆盖自动身份推断。

---

## 免责声明

本工具所有分析基于**微博公开数据**自动生成：
- 定位为"社交行为模式观察"，**不是人格鉴定、心理学评估或人际关系建议**
- 匹配得分和判词仅供娱乐，不构成任何真实关系的评判依据
- 分析他人数据需由用户自己确认合法性和正当性
- 不访问、不存储任何隐私数据（私信、仅好友可见等）
